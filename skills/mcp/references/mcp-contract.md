# MCP Contract

MCP discovery is dynamic. The current `mcp.status` and `mcp.discover` results outrank skill text, memory, and model knowledge.

A discovered tool is not evidence that execution succeeded. Each execution requires a completed `mcp.execute` result. Never expose MCP credentials or raw authorization configuration.
