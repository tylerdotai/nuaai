# Provider Contract

Model download, model availability, and active model selection are separate facts. Verify each independently.

A provider status result supports claims about the current runtime only at the time returned. A model response does not prove which model generated it; use the run/provider metadata.
